[project]
name = "week4-workshop-env"
version = "0.1.0"
requires-python = ">=3.10"

[tool.uv.dev-dependencies]
dependencies = [
    "langchain",
    "langgraph",
    "langchain-openai",
    "langchain-anthropic",
    "langchain-community",
]

